import 'package:flutter/material.dart';
// import 'package:get/get.dart';

class ScanFinger extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Material(
        child: Center(
          child: Container(
            child: Text("ScanFinger"),
          ),
        ),
      ),
    );
  }
}
